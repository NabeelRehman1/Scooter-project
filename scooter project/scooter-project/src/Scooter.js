const ScooterApp = require("./ScooterApp")

class Scooter{
 constructor (station, user, serial, nextserial, charge, isBroke){
  this.station - station
  this.user - user
  this.serial-  nextserial
  this.charge - charge
  this.isBroke = isBroke
 }
}
rent ()
  if (this.change > 20 && this.isBroke --- false){
    this.station === null;

  } else {
    this.user === null;
    if (this.charge < 20) {
      throw new Error ("needs to charge")

    }else if (this.isBroke === true) {
      throw new error ("needs repair")
    }
  };
module.exports = Scooter
